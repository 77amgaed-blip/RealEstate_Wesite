مشروع BS_BIM 🏗️

  ملخص قصير:موقع ويب أحادي (PHP + MySQL) بالعربية لعرض خدمات شركـة BS_BIM ولوحة إدارة بسيطة لإدارة المشاريع.

---

  ✅ المتطلبات والتشغيل السريع
- بيئة تطوير: XAMPP (Apache + MySQL) على Windows أو أي بيئة PHP + MySQL.
- PHP 7.4+ وملحق MySQLi.

الخطوات السريعة:
1. ضع المجلد داخل مجلد الويب (مثال: `C:\xampp\htdocs\bs_bim_bew11`).
2. شغّل Apache وMySQL عبر XAMPP.
3. استورد قاعدة البيانات:
   ```bash
   mysql -u root -p db_bs_bim < database/db_bs_bim.sql
   ```
4. افتح `http://localhost/bs_bim_bew11/` في المتصفح.
5. راجع إعدادات الاتصال في `conect/counect.php` إن لزم.

---

 🔎 الهيكل العام والملفات المهمة
- صفحات الواجهة: `index.php`, `file/header.php`, `file/footer.php`.
- لوحة الإدارة: `admin_login.php`, `admin_projects.php`, `delete_project.php`.
- اتصال بقاعدة البيانات: `conect/counect.php`.
- بيانات/أصول: `css/`, `js/`, `images/`, `data/`.
- سكريبتات: `js/comments-system.js` ↔ `comments.php`, و`pross/send-email.php` لبريد التواصل.
- سكيما DB: `database/db_bs_bim.sql`.

---

 ⚠️ ملاحظات وصعوبات معروفة
- أخطاء تهجئة في الاستدعاءات (مثال: `contect.php` vs `counect.php`) — راجع `pross/send-email.php`.
- `comments.php` موضوع داخل تعليق HTML؛ الواجهة الجاڤاسكربتية تتوقع استجابة نصية؛ يجب تفعيل الملف.
- الأمان: معظم الاستعلامات غير محضّرة → عرضة لـ SQL Injection؛ كلمات مرور المخزنّة كنص صريح.
- رفع الصور: فحص الامتدادات والحجم موجود جزئيًا (5MB) لكن ينصح التحقق من نوع MIME.

---

 ✍️ إصلاحات ونصائح عملية (أمثلة)
- تصحيح include:
  ```php
  // في pross/send-email.php
  require('../conect/counect.php');
  ```
- تمكين نقطة التعليقات: احذف التعليقات حول `comments.php` وتأكد أن الاستجابة تستخدم `text/plain; charset=UTF-8` وتُرجع نصًا متوقعًا مثل `تم إرسال التعليق بنجاح`.
- تحويل استعلام تسجيل الدخول إلى Prepared Statement:
  ```php
  $stmt = mysqli_prepare($conn, 'SELECT * FROM login WHERE username=? AND password=?');
  mysqli_stmt_bind_param($stmt, 'ss', $admin_username, $admin_password);
  mysqli_stmt_execute($stmt);
  ```
- تحسين رفع الصور: استخدم `mime_content_type()` واسم ملف آمن قبل `move_uploaded_file()`.

---

 🔧 أدوات تصحيح الأخطاء
- تفعيل عرض الأخطاء في التطوير: `php.ini` -> `display_errors = On` أو `ini_set('display_errors', 1);`.
- سجلات Apache/PHP: `xampp\apache\logs`، `xampp\php\logs`.
- لاختبار البريد محليًا استخدم MailHog أو اختبارات SMTP محلية.

---

 🤝 إرشادات للمساهمين
- افتح Issue قبل PR، واختصر التغييرات بــ PR لكل إصلاح.
- لا تغير محتوى الواجهة العربي بدون موافقة العميل.
- ضمّن اختبار يدوي (التحقق من رفع صور، تسجيل الدخول، تعليق، وإرسال بريد) لكل PR تغييري.

---

هل تريد أن أُجري الآن: 1) تصحيح استدعاء الملفات؟ 2) تفعيل `comments.php`؟ 3) مثال لتحويل استعلام إلى Prepared Statements؟ أجب برقم الخيار.
